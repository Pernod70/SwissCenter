-- -------------------------------------------------------------------------------------------------
-- Add the sync column to the media_locations table
-- -------------------------------------------------------------------------------------------------

ALTER TABLE media_locations ADD COLUMN sync CHAR(1) default 'N';